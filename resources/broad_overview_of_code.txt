struct image {
	uint32_t height;
	uint32_t width;
	uint8_t* channels; // number of coloUr systems in it (3 = rgb, 1 = greyscale)
	uint8_t* img;
};


Block detector: 

bool block_in_image(image source_img, image* block_mask);


Colour detector:

struct colour {
	uint8_t r.g.b;
};

int detect_colour(image src, image block_mask);


Block recognizer: 

struct block_info;